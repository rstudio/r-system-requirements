{
  "name": "rsysreqs-db",
  "version": "0.1.0",
  "description": "Database of System Requirements for R Packages",
  "scripts": {
    "test": "npm run lint && npm run validate",
    "lint": "eslint sysreqs/**",
    "validate": "ajv -s schema.json -d 'sysreqs/*.json'"
  },
  "license": "MIT",
  "dependencies": {
    "ajv-cli": "^3.0.0",
    "eslint": "^4.19.1",
    "eslint-plugin-json": "^1.2.0"
  }
}
